{% from "_macros/links" import linkCta %}

{% set childBlocks = block.children.collect() %}

{# Output #}
<div class="[{{ block.type.handle }}] space-y-8">
  <div>
    {% if childBlocks | length %}
      {% for childBlock in childBlocks %}
        {% include  '_blocks/' ~ childBlock.type ignore missing with { block: childBlock } only %}
      {% endfor %}
    {% endif %}
  </div>
  <div>
    {{ linkCta(block.linkField, {}, false, '', 'block-cta') }}
  </div>
</div>
